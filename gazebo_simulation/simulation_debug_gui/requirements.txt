# GUI + plotting
PySide6>=6.5         # PyQt6 also works. PyQt5 works too but needs unscoped-enum tweaks in gui.py
pyqtgraph>=0.13
numpy>=1.23
PyOpenGL>=3.1        # for the 3D lidar point cloud (LIDAR_VIEW = "3d")
# PyOpenGL_accelerate  # optional, speeds up GL; skip if it fails to build

# NOTE: the Gazebo Python bindings (gz.transportXX / gz.msgsXX) are NOT on PyPI.
# They ship with your Gazebo install. Install via your distro, e.g. for Harmonic:
#   sudo apt install python3-gz-transport13 python3-gz-msgs10
# (and source /opt/ros or the gazebo setup so the modules are importable).